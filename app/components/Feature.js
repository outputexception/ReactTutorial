var React = require('react');

var Feature = React.createClass({

  render: function() {
    return (
      <div>Message from Feature
      </div>
    );
  }

});

module.exports = Feature;
